---
/** Renders the GLX mark via the #glx-mark symbol defined in BaseLayout. */
const { class: className, ...rest } = Astro.props;
---

<svg class={className} {...rest}><use href="#glx-mark"></use></svg>
